const MessageBus = require('../app/message_bus'),
  assert = require('assert'),
  sinon = require('sinon');


describe("MessageBus", function() {
  it("closes connections without valid profiles");
  it("joins connections to their profile rooms");
});
